数组的排序：
	使用sort（function）的方法：
	例如：var arr = [2,34,56,1,8,11,89];
    arr.sort(function (a,b) {
        if(a>b){
            return 1
        }else if(a=b){
            return 0
        }else if(a<b){
            return -1
        }
    })
    console.log(arr)