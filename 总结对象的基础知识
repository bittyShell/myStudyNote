对象的定义方法：
	第一种：var 对象名 = {
	  属性名：属性值，
	  方法名：function（）{

	  }
	}
	第二种：var 对象名 = new Object()
		对象名.属性名 = 属性值;
		对象名.方法名 = function（）{

		};
	第三种：先创建一个已有对象 
		var obj = {  }
		var 对象名 = Object.create(obj);
		特点：新的对象会继承前一个对象的属性与方法；
对象的调用方式：
	第一种：
		对象名.属性名
		对象名.方法名（）；
	第二种：
		对象名['属性名']；
		对象名['方法名']（）；
对象属性与方法的遍历：
	第一种：for(var 变量名 in 对象名){
		console.log(变量名)；
		console.log（对象名[变量名]）；
	}
	第二种：var 变量名 = Object.keys(对象名)；
			console.log(变量名)---只能打印属性名和方法名
			console.log(变量名[数字序号])---打印相应序号属性的属性值或方法
	判断属性是否存在：
		第一种：if（对象名.属性名）{
			console.log(对象名.属性名)
		} else {
		console.log('该属性不存在'）
		}
		第二种：console.log('属性名' in 对象名)---true or false
对象的操作：
	增：对象名.新增的属性名 = '属性值'；方法也一样
	改：对象名.之前的属性名 = '改变的属性值';
	删：delete 对象名['属性名/方法名']；
